import 'package:flutter/material.dart';

class Angoleta extends StatelessWidget {
  const Angoleta({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Info Angoleta");
  }
}
